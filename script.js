// Skrypt nie jest wymagany do animacji obrotu, 
// ponieważ jest obsługiwana przez CSS

document.addEventListener('DOMContentLoaded', function() {
    // Przyszłe interakcje mogą być dodane tutaj
});
